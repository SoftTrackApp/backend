openapi: 3.0.4
info:
  title: SoftTrack API
  version: 0.1.1

paths:
  /users:
    post:
      tags:
        - users
      summary: Create user
      description: This can only be done by the user with `can_manage_users` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '409':
          description: Username is taken already

    get:
      tags:
        - users
      summary: List all users
      description: This can only be done by the user with `can_manage_users` permission.
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /users/{id}:
    get:
      tags:
        - users
      summary: Get user by ID
      description: This can only be done by the user with `can_manage_users` permission.
      responses:
        '200':
          description: A single user
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    put:
      tags:
        - users
      summary: Update user information
      description: This can only be done by the user with `can_manage_users` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdate'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    delete:
      tags:
        - users
      summary: Delete user
      description: This can only be done by the user with `can_manage_users` permission.
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

  /users/login:
    post:
      tags:
        - users
      summary: Log into user account
      description: Saves JWT token in the cookie if logged in successfully.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserLogin'
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized

  /groups:
    post:
      tags:
        - groups
      summary: Create group
      description: This can only be done by the user with `can_manage_groups` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GroupCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '409':
          description: Object with this name already exists

    get:
      tags:
        - groups
      summary: List all groups
      description: This can only be done by the user with `can_manage_groups` permission.
      responses:
        '200':
          description: List of groups
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Group'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /groups/{id}:
    get:
      tags:
        - groups
      summary: Get group by ID
      description: This can only be done by the user with `can_manage_groups` permission.
      responses:
        '200':
          description: A single group
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    put:
      tags:
        - groups
      summary: Update group
      description: This can only be done by the user with `can_manage_groups` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GroupUpdate'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Group'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    delete:
      tags:
        - groups
      summary: Delete group
      description: This can only be done by the user with `can_manage_groups` permission.
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

  /roles:
    post:
      tags:
        - roles
      summary: Create role
      description: This can only be done by the user with `can_manage_roles` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '409':
          description: Object with this name already exists

    get:
      tags:
        - roles
      summary: List all roles
      description: This can only be done by the user with `can_manage_roles` permission.
      responses:
        '200':
          description: List of roles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Role'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /roles/{id}:
    get:
      tags:
        - roles
      summary: Get role by ID
      description: This can only be done by the user with `can_manage_roles` permission.
      responses:
        '200':
          description: A single role
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    put:
      tags:
        - roles
      summary: Update role
      description: This can only be done by the user with `can_manage_roles` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RoleUpdate'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    delete:
      tags:
        - roles
      summary: Delete role
      description: This can only be done by the user with `can_manage_roles` permission.
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

  /records:
    post:
      tags:
        - records
      summary: Create record
      description: This can only be done by the user with `can_manage_records` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Record'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

    get:
      tags:
        - records
      summary: List all records
      description: This can only be done by the user with `can_manage_records` permission.
      responses:
        '200':
          description: List of records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Record'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /behaviors:
    post:
      tags:
        - behaviors
      summary: Create a behavior
      description: This can only be done by the user with `can_manage_behaviors` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Behavior'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '409':
          description: Object with this name already exists

    get:
      tags:
        - behaviors
      summary: List all behaviors
      description: This can only be done by the user with `can_manage_behaviors` permission.
      responses:
        '200':
          description: List of behaviors
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Behavior'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /behaviors/{id}:
    get:
      tags:
        - behaviors
      summary: Get behavior by ID
      description: This can only be done by the user with `can_manage_behaviors` permission.
      responses:
        '200':
          description: A single behavior
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Behavior'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    put:
      tags:
        - behaviors
      summary: Update a behavior
      description: This can only be done by the user with `can_manage_behaviors` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BehaviorUpdate'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Behavior'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    delete:
      tags:
        - behaviors
      summary: Delete a behavior
      description: This can only be done by the user with `can_manage_behaviors` permission.
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

  /skills:
    post:
      tags:
        - skills
      summary: Create a skill
      description: This can only be done by the user with `can_manage_skills` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkillCreate'
      responses:
        '201':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Skill'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '409':
          description: Object with this name already exists

    get:
      tags:
        - skills
      summary: List all skills
      description: This can only be done by the user with `can_manage_skills` permission.
      responses:
        '200':
          description: List of skills
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Skill'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden

  /skills/{id}:
    get:
      tags:
        - skills
      summary: Get skill by ID
      description: This can only be done by the user with `can_manage_skills` permission.
      responses:
        '200':
          description: A single skill
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Skill'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    put:
      tags:
        - skills
      summary: Update a skill
      description: This can only be done by the user with `can_manage_skills` permission.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SkillUpdate'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Skill'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

    delete:
      tags:
        - skills
      summary: Delete a skill
      description: This can only be done by the user with `can_manage_skills` permission.
      responses:
        '200':
          description: Success
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Not found

components:
  schemas:
    User:
      properties:
        id:
          type: integer
        username:
          type: string
        first_name:
          type: string
          nullable: true
        last_name:
          type: string
          nullable: true
        role_id:
          type: number
        groups:
          type: array
          items:
            $ref: '#/components/schemas/Group'
    UserCreate:
      properties:
        username:
          type: string
        password:
          type: string
        first_name:
          type: string
          nullable: true
        last_name:
          type: string
          nullable: true
        role_id:
          type: number
          nullable: true
        groups:
          type: array
          items:
            type: number
          nullable: true
    UserUpdate:
      properties:
        username:
          type: string
        first_name:
          type: string
          nullable: true
        last_name:
          type: string
          nullable: true
        role_id:
          type: number
          nullable: true
        groups:
          type: array
          items:
            type: number
          nullable: true
    UserLogin:
      properties:
        username:
          type: string
        password:
          type: string

    Group:
      properties:
        id:
          type: integer
        name:
          type: string
    GroupCreate:
      properties:
        name:
          type: string
    GroupUpdate:
      properties:
        name:
          type: string

    Role:
      properties:
        id:
          type: integer
        name:
          type: string
    RoleCreate:
      properties:
        name:
          type: string
    RoleUpdate:
      properties:
        name:
          type: string

    Record:
      properties:
        id:
          type: integer
        name:
          type: string
        group_id:
          type: number
        behavior_set_id:
          type: number
        comment:
          type: string
          nullable: true
        created_at:
          type: number
    RecordCreate:
      properties:
        name:
          type: string
        group_id:
          type: number
        behavior_set_id:
          type: number
        comment:
          type: string
          nullable: true

    Behavior:
      properties:
        id:
          type: integer
        name:
          type: string
        skills:
          type: array
          items:
            type: number
    BehaviorCreate:
      properties:
        name:
          type: string
        skills:
          type: array
          items:
            type: number
    BehaviorUpdate:
      properties:
        name:
          type: string
        skills:
          type: array
          items:
            type: number

    Skill:
      properties:
        id:
          type: integer
        name:
          type: string
        behaviors:
          type: array
          items:
            type: number
    SkillCreate:
      properties:
        name:
          type: string
        behaviors:
          type: array
          items:
            type: number
    SkillUpdate:
      properties:
        name:
          type: string
        behaviors:
          type: array
          items:
            type: number
